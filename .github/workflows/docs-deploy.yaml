name: 📚 Deploy Documentation

on:
  workflow_call:
    inputs:
      tag:
        description: Existing global release tag to deploy
        required: true
        type: string
  workflow_dispatch:
    inputs:
      tag:
        description: Existing global release tag to deploy
        required: true
        type: string

permissions:
  contents: write

jobs:
  deploy:
    uses: ShawnDen-coder/repo-scaffold/.github/workflows/reusable-docs-deploy.yaml@0.27.1
    with:
      tag: ${{ inputs.tag }}
      create-release: true
      artifact-name: dist
    secrets:
      PYPI_SERVER_USERNAME: ${{ secrets.PYPI_SERVER_USERNAME }}
      PYPI_SERVER_PASSWORD: ${{ secrets.PYPI_SERVER_PASSWORD }}
