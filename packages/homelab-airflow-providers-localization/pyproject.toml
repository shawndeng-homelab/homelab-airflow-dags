[project]
name = 'homelab-airflow-providers-localization'
version = '0.1.0'
description = 'Apache Airflow provider for the homelab video localization service'
readme = 'README.md'
authors = [
    { name = 'Shawn Deng', email = 'shawndeng1109@qq.com' }
]
license = 'MIT'
requires-python = '>=3.12, <3.12.99'
dependencies = [
    'aiohttp>=3.10.0',
    'apache-airflow==2.11.0',
    'homelab-video-contracts>=0.1.0,<0.2.0',
    'requests>=2.32.0',
]

[tool.uv.sources]
homelab-video-contracts = { workspace = true }

[project.entry-points.'apache_airflow_provider']
provider_info = 'homelab_airflow_providers_localization.get_provider_info:get_provider_info'

[build-system]
requires = ['hatchling']
build-backend = 'hatchling.build'

[tool.hatch.build.targets.wheel]
packages = ['src/homelab_airflow_providers_localization']
